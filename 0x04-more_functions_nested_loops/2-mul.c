#include "main.h"
/**
 * mul- function to add two integers
 * @a: number one
 * @b: number two
 * Return:multiply number one and number two
 */

int mul(int a, int b)
{                                                         return (a * b);
}
