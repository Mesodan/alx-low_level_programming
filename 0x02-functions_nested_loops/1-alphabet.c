#include "main.h"
/**
 * print_alphabet - lowercase
 * by Mesodan
 */

void print_alphabet(void)
{
	char ch;
	for (ch = 'a', ch <= 'z'; ch++)
	{
		_putchar(ch);
	}
	{
	_putchar('\n');
	}
	return (0);
}
