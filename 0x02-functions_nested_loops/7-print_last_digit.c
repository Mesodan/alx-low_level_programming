#include "main.h"

/**
 * print_last_digit- to print last digit
 *
 * @j: introduced parmater
 * Return: w
 */

int print_last_digit(int j)

{
	int w;

	w = j % 10;
	if (j < 0)
	{
		w = -w;
	}
	return (w);
	_putchar(r+'0');
}
