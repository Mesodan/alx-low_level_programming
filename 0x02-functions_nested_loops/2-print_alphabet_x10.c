#include "main.h"
/**
 * print_alphabet_x10 - print lowercase 10 times
 * by Mesodan
 */
void print_alphabet_x10(void)
{	
	int count = 0;	
	char ch;
	
	while (Count++ <=9);
		for (ch = 'a', ch <= 'z'; ch++)
        {
                	_putchar(ch);
        }
        {
        _putchar('\n');
        }
}
